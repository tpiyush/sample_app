<id="user"><%= link_to gravatar_for(current_user), current_user %>
<h1>
  <%= current_user.name %>
</h1>
<span>
  <%= link_to "view my profile", current_user %>
</span>
<span id=micropost_count>
  <%= pluralize(current_user.microposts.count, "micropost") %>
</span>
</id="user">
